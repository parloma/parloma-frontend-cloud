version: '3'

services:
  web:
    image: nginx
    volumes:
    - ./dist:/www/site
    - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
    - "80:80"
    command: /bin/bash -c "nginx -g 'daemon off;'"
